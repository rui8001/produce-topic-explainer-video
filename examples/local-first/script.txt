挑一个笔记工具，先做个小实验：断开网络，写一句话，再关掉重开。如果只能看旧内容，不能继续编辑，它满足的可能只是缓存需求。

本地优先把设备上的数据副本放在核心位置，联网主要帮助同步。它不等于永远离线，也不等于买一个带“本地”标签的产品就自动获得全部能力。

两个人离线修改同一个字段，重新连接后怎么办？以 Automerge 为例，冲突值可以被保留下来。数据合并有规则，人的不同意图仍需要产品解释清楚。

选工具时，拿一份不含隐私的测试文档，检查离线编辑、导出后能否重开、冲突结果是否看得懂。这是选择方法，不是对任何产品安全性或数据永不丢失的保证。
